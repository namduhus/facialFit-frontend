import 'dart:convert';
import 'dart:ffi';
import 'dart:typed_data';
import 'package:flutter/material.dart';
import 'package:http/http.dart' as http;
import 'package:shared_preferences/shared_preferences.dart';

class Getpredict {
  //url = "http://203.241.246.109:10005/predict";

  Future<void> predict(Uint8 bytes) async {
    /*
    final response = await http.post(Uri.parse(url), headers: <Uint8>{
      '': '', 
    },
    body: jsonEncode(<Uint8>{
      '':'',
      
    }),
    
    );
*/
    //성공

    //실패
  }
}
